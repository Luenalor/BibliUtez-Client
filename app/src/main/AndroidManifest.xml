<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.alexl.bibliutez">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:networkSecurityConfig="@xml/network_security_config"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true">
        <activity android:name=".ClienteHistorial"></activity>
        <activity android:name=".ListaLibrosActivity" />
        <activity android:name=".RegistroLibros" />
        <activity android:name=".RegistroCliente" />
        <activity android:name=".ModificarLibros" />
        <activity android:name=".GerenteMenuPrincipal" />
        <activity android:name=".GerenteGestionLibros" />
        <activity android:name=".ClientePerfil" />
        <activity android:name=".ClienteMenuPrincipal" />
        <activity android:name=".CarritoCompras" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>